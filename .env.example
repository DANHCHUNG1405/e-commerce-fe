# Browser calls Next.js proxy to avoid backend CORS restrictions.
NEXT_PUBLIC_API_URL=
BACKEND_API_URL=
# Browser connects directly to backend WebSocket (not the HTTP rewrite).
# Production: wss://your-backend.example/api/v1/ws
# Configure backend WEBSOCKET_ORIGINS to allow the frontend origin.
NEXT_PUBLIC_WS_URL=
